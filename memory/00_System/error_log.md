# JARVIS Error Log (错题集)

## [2026-03-03 09:00 CST] 早报自动任务执行失败
- **问题现象**：北京时间 09:00 未能按时推送 AI 行业早报。
- **根因分析**：
  1. **定时器挂载丢失**：由于前日执行了 `/clear` 清理上下文并重启网关，内存中的 Cron 任务未能正确重新注册。
  2. **缺少自检触发**：Heartbeat 仅检查 Token 负载，未包含“关键定时任务生存检查”。
- **修复动作**：
  1. 将“早报生存状态”加入 Heartbeat 检查清单。
  2. 确立“网关重启后强制重新初始化 Cron”的标准流程。

## [2026-03-03 11:41 CST] 子代理派生权限被拦截 (agentId forbidden)
- **问题现象**：调用 Claude 3.5 Sonnet 生成早报时，系统报错并终止任务。
- **根因分析**：
  1. **配置回滚**：手动修改的 `openclaw.json` 权限配置被系统 `doctor` 自动纠正/覆盖。
  2. **授权缺失**：主代理 `main` 下未显式配置 `subagents.allow` 列表。
- **修复动作**：
  1. 强制在 `openclaw.json` 中锁定 `"allow": ["coder", "claude-proxy"]`。
  2. 建立“配置锁定审计”，若发现 `allow` 列表变动，Heartbeat 必须立即报警。

## [2026-03-03 12:05 CST] 搜索功能不可用 (missing_brave_api_key)
- **问题现象**：`web_search` 技能报错，提示缺少 API Key。
- **根因分析**：Key 未能持久化在配置文件中，仅存在于会话内存或已过期的环境变量中。
- **修复动作**：
  1. 临时切换搜索链路至 **Exa AI**（Key 已确认有效）。
  2. 持久化存储 Exa 配置，并在 `openclaw.json` 中备份。

---
*Generated by JARVIS at 2026-03-03 13:57 CST*
